// An Interface is a contract, any class that implements an 
// Interface, the Interface wants that class to promise that
// it will implement that method.
public interface Flyable {
	
	// Interfaces does not have bodies
	
	// abstract method
	public void fly();

}
